apiVersion: v1
kind: LimitRange
metadata: # kpt-merge: istions/istions
  name: istions
  namespace: istio-backend1
spec:
  limits:
  - type: Container
    max:
      cpu: '1'
  - type: Pod
    max:
      cpu: '4'
      memory: 4G
